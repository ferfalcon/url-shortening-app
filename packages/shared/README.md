# Shared Package

This folder is reserved for stable types, schemas, and tiny utilities that are genuinely shared by both `apps/web` and `apps/api`.

It is intentionally empty for now beyond this note so the repository does not drift into using `packages/shared` as a dumping ground.
